---
id: 2026-06-11-002
baslik: "UI paket: tek entry point — alt-modül export'larını kaldır"
proje: panel
grup: null
durum: arsiv
oncelik: yuksek
pr: null
olusturulma: 2026-06-11T14:06:16.061Z
guncelleme: 2026-06-11T15:06:26.169Z
gecmis: 2026-06-11T14:13:49.610Z|geldi>spec;2026-06-11T14:36:58.693Z|spec>kuyruk;2026-06-11T14:40:23.370Z|kuyruk>yapiliyor;2026-06-11T14:55:40.999Z|yapiliyor>inceleme;2026-06-11T15:06:18.050Z|inceleme>bitti;2026-06-11T15:06:26.169Z|bitti>arsiv
---
Feature Export Mimarisi spec'i — Adım 2/7. Önce 2026-06-11-001 tamamlanmış olmalı.

- package.json exports sadece üç giriş: '.' → dist/index.js, './tokens.css', './styles.css'. Başka sub-path export YOK.
- /wraps, /custom, /icons, /charts, /theme sub-path'leri kaldırılır (bileşenler kalır, sadece import yolu değişir).
- src/index.ts tek entry: tüm public export'lar buradan (primitifler + feature bileşenleri + servicecoreTheme + i18n key tipleri).
- Playground'daki tüm import'lar tek entry'ye geçirilir: import { Button, AuthShell, servicecoreTheme } from '@servicecoreui/ui'.

Doğrulama: pnpm build temiz, playground tüm sayfalar çalışır.

## Spec

### Açık Sorular
1. **RSC "use client" tainting:** Mevcut mimari `index.ts`'i kasıtlı olarak server-safe tutuyor (typography + theme, AntD yok), AntD wrap'leri ayrı `/wraps` bundle'ında `"use client"` banner'lı. Tek entry'ye birleştirince `index.js` AntD + Recharts içerecek → tüm bundle `"use client"` olmak zorunda kalır; bu, typography primitiflerinin Next.js **server component**'lerinde import edilmesini bozar. Varsayım: 001'de bu karar verildi ve tüketim tamamen client-side kabul ediliyor. Değilse, server-safe alt-entry korunmalı (görevle çelişir). **Bu spec, tek entry + global `"use client"` banner varsayımıyla yazıldı.**
2. **Icons:** `/icons` (`@carbon/icons-react` re-export) kaldırılıyor. Carbon ikonları tek entry'den mi re-export edilecek (`export * from "@carbon/icons-react"`) yoksa tüketici Carbon'u doğrudan mı kuracak? Bu spec, **tek entry'den re-export** varsayar (playground'da 1 kullanım var, kırılmasın diye).
3. **`./theme/tokens.css`:** Mevcut pakette hem `./tokens.css` hem `./theme/tokens.css` aynı dosyaya işaret ediyor. Görev üç entry diyor → yalnız `./tokens.css` kalır, `./theme/tokens.css` kaldırılır (kullanan yoksa güvenli).

### Amaç
`@servicecoreui/ui` paketini tek public giriş noktasına indirgemek. `package.json` `exports` yalnız üç anahtar barındıracak: `.` (→ `dist/index.js`/`.cjs`/`.d.ts`), `./tokens.css`, `./styles.css`. `/wraps`, `/custom`, `/icons`, `/charts`, `/theme` sub-path'leri kalkar. Bileşenler/dosyalar **silinmez**; yalnız barrel topolojisi ve import yolları değişir. `src/index.ts` tüm public API'nin tek kaynağı olur. Playground bütün import'larını tek entry'ye taşır.

### Kapsam ve Mevcut Durum
- Entry barrel'ları: `src/index.ts` (server-safe), `src/wraps.ts` (57 AntD wrap), `src/custom.ts` (16 özel/composite + AuthShell/PasswordChecklist/SystemMessage/SettingsForm), `src/charts.ts` (4 grafik), `src/icons.ts` (Carbon re-export), `src/theme/index.ts`.
- Build: `build.mjs` server-safe tier (index/icons/theme, banner yok) + client tier (wraps/custom/charts, `"use client"` banner) üretiyor. Tek entry'de bu ayrım kalkar → tüm `index` çıktısı `"use client"` banner'lı olur.
- Playground tüketimi: `@servicecoreui/ui` 279, `/wraps` 118, `/custom` 34, `/charts` 23, `/theme` 6, `/icons` 1 (+ `/styles` 11, `/tokens` 6 — bunlar CSS, korunur).

### Yaklaşım
1. `src/index.ts`'i tek barrel yap: mevcut typography + theme re-export'larına ek olarak `wraps`, `custom`, `charts`, `icons` barrel'larının içeriğini de re-export et (`export * from "./wraps"` vb. ya da barrel'ları doğrudan index'e inline et). Yeni eklenenler: i18n key tipleri (görevde belirtilen — varsa kaynağını bul, yoksa atla/işaretle).
2. `build.mjs`'i tek entry'ye indir: `index` (+ opsiyonel `theme/index` iç bağımlılık olarak ama ayrı çıktı değil) tek esbuild entry; tüm bundle `"use client"` banner'lı; `icons`/`wraps`/`custom`/`charts` ayrı entryPoint'leri kaldır. CSS toplama mantığı korunur (tek `styles.css`).
3. `package.json` `exports`'u üç anahtara indir; `main`/`module`/`types` korunur.
4. Playground'da tüm sub-path import'larını `@servicecoreui/ui`'ye çevir (sadece JS sub-path'leri; `/styles.css` ve `/tokens.css` CSS import'larına dokunma).
5. Doğrula: `pnpm build` temiz, playground tüm sayfalar derlenip çalışır.

### Doğrulama (genel)
- `pnpm --filter @servicecoreui/ui build` hatasız; `dist/`'te yalnız `index.{js,cjs,d.ts}`, `styles.css`, `theme/tokens.css`/`tokens.css` üretilir, ayrı `wraps.*`/`custom.*`/`charts.*`/`icons.*` çıktısı **yok**.
- Playground'da `@servicecoreui/ui/{wraps,custom,charts,icons,theme}` deseni `grep` ile **sıfır** sonuç.
- `pnpm --filter playground build` temiz; tüm sayfalar (auth, ayarlar, sistem, charts, wraps demoları) derlenir.

## Alt-tasklar
- [ ] 001 (`2026-06-11-001`) tamamlanmış mı doğrula — bu adımın ön koşulu. — doğrulama: 001 görev durumu "done"; değilse dur ve bildir.
- [ ] i18n key tiplerinin kaynağını tespit et (`src/` içinde i18n/key tipi var mı?). — doğrulama: `grep -rn "i18n\|I18nKey\|MessageKey" panel/packages/ui/src` ile kaynak bulunur ya da "yok" tespiti netleşir; index'e ekleme planı bu sonuca göre.
- [ ] `src/index.ts`'i tek barrel'a genişlet: mevcut typography + theme export'larına `wraps`, `custom`, `charts`, `icons` içeriğini ve (varsa) i18n key tiplerini ekle; `servicecoreTheme` zaten re-export ediliyor, koru. — doğrulama: `grep -c "export \* from" src/index.ts` beklenen barrel sayısını verir; çakışan isim export'u yok (`tsc --noEmit` ileride teyit).
- [ ] `build.mjs`'i tek entry'ye indir: `wraps`/`custom`/`charts`/`icons` entryPoint'lerini kaldır, `index`'i (artık AntD içerdiği için) `"use client"` banner'lı tek bundle yap; CSS toplama korunur. — doğrulama: `node build.mjs` çalışır; `dist/`'te `index.{js,cjs}` var, `wraps.*`/`custom.*`/`charts.*`/`icons.*` **yok**; `head -1 dist/index.js` → `"use client";`.
- [ ] `package.json` `exports`'u üç anahtara indir (`.`, `./tokens.css`, `./styles.css`); `./wraps`/`./custom`/`./charts`/`./icons`/`./theme`/`./theme/tokens.css` anahtarlarını kaldır; `main`/`module`/`types` koru. — doğrulama: `node -e "console.log(Object.keys(require('./package.json').exports))"` → tam olarak `['.', './tokens.css', './styles.css']`.
- [ ] Paketi tek başına derle ve tip/dts çıktısını doğrula. — doğrulama: `pnpm --filter @servicecoreui/ui build` çıkış kodu 0; `dist/index.d.ts` üretildi ve tüm public sembolleri içeriyor.
- [ ] (Opsiyonel ama önerilir) Eski barrel dosyalarını (`wraps.ts`/`custom.ts`/`charts.ts`/`icons.ts`) iç modül olarak bırak (index onları re-export ediyorsa) ya da içeriklerini index'e taşıyıp sil — seçilen stratejiyi tutarlı uygula. — doğrulama: `tsc --noEmit` temiz; ölü/erişilmez barrel kalmadı.
- [ ] Playground'daki tüm sub-path JS import'larını tek entry'ye çevir (`/wraps`,`/custom`,`/charts`,`/icons`,`/theme` → `''`); CSS import'larına (`/styles.css`, `/tokens.css`) dokunma. — doğrulama: `grep -rn "@servicecoreui/ui/\(wraps\|custom\|charts\|icons\|theme\)" apps/playground/src` → 0 sonuç.
- [ ] Playground'u derle ve tüm sayfaları kontrol et. — doğrulama: `pnpm --filter playground build` çıkış kodu 0; dev sunucuda auth/ayarlar/sistem/charts/wraps sayfaları hatasız render.
- [ ] Monorepo genel doğrulama: lint + tip kontrolü. — doğrulama: `pnpm --filter @servicecoreui/ui typecheck` ve playground lint temiz; CLAUDE.md "bitti demeden önce lint" kuralı sağlanır.

## Worker Raporu (2026-06-11T14:55)

---

SONUC: TAMAM

## Rapor

### Değişen Dosyalar

**`panel/packages/ui/`**
- `src/index.ts` — `export * from "@carbon/icons-react"` (doğrudan, `./icons` yerine), `DisplayProps/DisplaySize` tipi eklendi, `"use client"` direktifi başa taşındı (zaten vardı)
- `build.mjs` — tek entry point (`index`), `"use client"` banner, wraps/custom/charts/icons ayrı entryPoint'leri kaldırıldı
- `package.json` — `exports` 3 anahtara indirildi: `.`, `./tokens.css`, `./styles.css`

**`panel/apps/playground/app/`**
- 94 dosyada sub-path import'ları (`/wraps`, `/custom`, `/charts`, `/icons`, `/theme`) `@servicecoreui/ui` tek entry'ye çevrildi
- `typography/page.tsx`, `tokens/page.tsx`, `page.tsx`, `kbd/page.tsx`, `not-found.tsx` — `"use client"` direktifi eklendi (AntD+Carbon client-bundle'dan import)

### Doğrulama Sonuçları

| Kontrol | Sonuç |
|---|---|
| `dist/index.js` başı | `"use client";` ✓ |
| `dist/` içinde `wraps.*` / `custom.*` / `charts.*` / `icons.*` | Yok ✓ |
| `package.json exports` anahtarları | `['.', './tokens.css', './styles.css']` ✓ |
| Sub-path import grep (playground) | 0 sonuç ✓ |
| `pnpm --filter @servicecoreui/ui typecheck` | Temiz (0 hata) ✓ |
| `pnpm --filter playground build` | Başarılı, tüm sayfalar prerender ✓ |

### Teknik Not
Carbon ikonları (`export * from "@carbon/icons-react"`) esbuild'in `external` mekanizmasıyla kullanılırken nested barrel üzerinden gidildiğinde webpack `__reExport()` pattern'ını statik analiz edemediği için `Add`, `Edit` vb. `undefined` oluyordu. Çözüm: `src/index.ts`'te `./icons` barrel yerine doğrudan `@carbon/icons-react`'ten re-export — bu sayede ESM çıktısında `export * from "@carbon/icons-react"` satırı korunuyor ve webpack named export'ları çözebiliyor.
