---
id: 2026-06-14-001
baslik: "SolveAI A209 senkron: pull + reindex + 29 kart yükle"
proje: gemma
grup: null
durum: arsiv
oncelik: yuksek
pr: null
olusturulma: 2026-06-14T11:27:32.729Z
guncelleme: 2026-06-14T12:20:04.905Z
gecmis: 2026-06-14T11:28:54.196Z|geldi>spec;2026-06-14T11:47:32.824Z|spec>kuyruk;2026-06-14T11:48:56.338Z|kuyruk>yapiliyor;2026-06-14T11:53:24.342Z|yapiliyor>inceleme;2026-06-14T12:20:03.006Z|inceleme>bitti;2026-06-14T12:20:04.905Z|bitti>arsiv
---
A209 cozum_ara işi laptop'ta bitti (bge→Qwen3 + temizlik + 29 KB kartı). Kod GitHub'da (adk-uyum-rename, son commit 357ec44) ama .db gitignore → indeks/kartlar Mac Mini'de lokal üretilmeli.

SIRAYLA:
1. git pull origin adk-uyum-rename
2. cd projects/servicecore/solveai/solve
3. python _reindex_temiz.py            # benzer.db reindex, ~16 dk (temizle degisti)
4. python _kart_yukle.py               # eski 900xxx kartlari temizle + 29 karti yukle + cozum reindex, ~30 sn
5. python benzer_anahtar_regresyon.py  # dogrula -> BUL 16/16 SUS 2/2 TOPLAM 18/18 cikmali

NOTLAR:
- Qwen3-Reranker-0.6B ilk calismada otomatik iner (~1.2GB).
- _kart_yukle.py Mac Mini'nin kendi 5 Gemma kartini (900011-015) SILER, laptop'in 29 kartini koyar (karar: 29 kart ana kaynak).
- Sira onemli: once reindex, sonra kart, sonra dogrula.
- Detay: wiki #377-379 (A209 cozum_ara).

## Spec

A209'da laptop'ta tamamlanan `cozum_ara` işinin (embedder `bge → Qwen3-Reranker-0.6B` geçişi, veri temizliği, 29 KB kartı) Mac Mini'ye senkronu. Kod GitHub'da (`adk-uyum-rename`, commit `357ec44`); ancak `.db` dosyaları gitignore'da olduğu için **indeks ve kartlar Mac Mini'de lokal yeniden üretilecek**. Bu bir veri-senkron görevi, kod değişikliği yok — sıra kritik: önce reindex, sonra kart yükleme, sonra doğrulama.

Çalışma dizini: `projects/servicecore/solveai/solve`. Başarı kriteri: `benzer_anahtar_regresyon.py` çıktısı **BUL 16/16 · SUS 2/2 · TOPLAM 18/18**.

Riskler / dikkat:
- `_kart_yukle.py` Mac Mini'nin lokal 5 Gemma kartını (900011–900015) **siler** — karar gereği 29 kart ana kaynak. Geri alınamaz; çalıştırmadan önce script içeriği teyit edilir.
- Qwen3-Reranker-0.6B ilk çalışmada otomatik iner (~1.2GB) — internet + disk gerekir.
- Reindex ~16 dk; kesinti olursa benzer.db yarım kalır, baştan çalıştırılır.

### Açık Sorular
1. **Branch uyuşmazlığı:** Mevcut lokal branch `solveai-data-sync`, son commit `b651f71`. Görev `adk-uyum-rename` (commit `357ec44`) pull'unu istiyor. Adım 1 `git pull origin adk-uyum-rename` mevcut branch'e merge yapar — önce `git checkout adk-uyum-rename` mı gerekli, yoksa bu branch'e merge mi isteniyor? (Spec'te checkout adımı ekledim; istenmiyorsa atlanır.)
2. **Kirli çalışma alanı:** `git status`'ta staged `.db` dosyaları var (`benzer.db`, `cozum.db`, `feedback.db`) + `.claude/settings.local.json`. Pull öncesi bunların ne yapılacağı (stash / commit / olduğu gibi bırak) netleştirilmeli. `.db` zaten lokal üretilecek; muhtemelen unstage edilmeli.
3. Beklenen 3 script (`_reindex_temiz.py`, `_kart_yukle.py`, `benzer_anahtar_regresyon.py`) şu an lokalde **yok** — pull ile gelecekleri varsayılıyor. Gelmezlerse görev adım 2'de durur.

## Alt-tasklar
- [ ] Çalışma alanını pull'a hazırla: `git status` ile staged `.db` + `settings.local.json` durumunu gör, gerekirse `git stash` veya unstage — doğrulama: `git status` çalışma ağacı pull'u engellemeyecek durumda (çakışacak staged değişiklik yok).
- [ ] Açık Soru 1'i çöz: doğru branch'e geç (`git checkout adk-uyum-rename`) veya kullanıcı onayıyla mevcut branch'e merge stratejisini belirle — doğrulama: `git branch --show-current` hedef branch'i gösteriyor.
- [ ] `git pull origin adk-uyum-rename` — doğrulama: `git log --oneline -1` çıktısı `357ec44` ile başlıyor.
- [ ] Yeni scriptlerin geldiğini doğrula — doğrulama: `ls projects/servicecore/solveai/solve/_reindex_temiz.py _kart_yukle.py benzer_anahtar_regresyon.py` üçü de mevcut.
- [ ] `_kart_yukle.py` içeriğini oku, 900011–900015 silme + 29 kart yükleme davranışını teyit et (yıkıcı adım, ön-kontrol) — doğrulama: script gerçekten eski 900xxx kartları siliyor ve 29 kart ekliyor; beklenmeyen başka silme yok.
- [ ] `cd projects/servicecore/solveai/solve` ve `python _reindex_temiz.py` çalıştır (~16 dk, Qwen3 ilk inişte ~1.2GB) — doğrulama: hatasız bitti, `benzer.db` güncellendi (dosya mtime yeni, reindex log "tamam" çıktısı).
- [ ] `python _kart_yukle.py` çalıştır (~30 sn) — doğrulama: log eski 900xxx kartların silindiğini + 29 kartın yüklendiğini + cozum reindex tamamlandığını gösteriyor; cozum.db kart sayısı 29.
- [ ] `python benzer_anahtar_regresyon.py` ile regresyon doğrula — doğrulama: çıktı **BUL 16/16 · SUS 2/2 · TOPLAM 18/18**; sapma varsa reindex/kart adımları sırayla tekrar incelenir.
- [ ] Sonucu wiki #377–379 (A209 cozum_ara) bağlamıyla kullanıcıya raporla — doğrulama: hangi adım çalıştı, kart sayısı ve 18/18 sonucu özetlendi.

## Worker Raporu (2026-06-14T11:53)

**BUL 16/16 · SUS 2/2 · TOPLAM 18/18** — başarı kriteri karşılandı.

---

SONUC: TAMAM

## Rapor

**Branch:** `solveai-data-sync` → `adk-uyum-rename` geçişi yapıldı, `git pull origin adk-uyum-rename` ile commit `357ec44` çekildi.

**Adımlar:**

1. **Hazırlık:** Staged `.db` dosyaları unstage edildi (LFS uyarısı geldi ama etkilemedi). `adk-uyum-rename` branch'ine geçildi ve pull yapıldı.

2. **Scriptler doğrulandı:** `_reindex_temiz.py`, `_kart_yukle.py`, `benzer_anahtar_regresyon.py` — üçü de mevcut.

3. **`_kart_yukle.py` teyit:** Sadece `cozum_id >= 900000` olanları siliyor, başka yıkıcı işlem yok — onaylandı.

4. **`_reindex_temiz.py`:** 8303 ticket yeniden indekslendi, 2.3 dk (Qwen3 model lokal bellekte zaten vardı, indirmedi).

5. **`_kart_yukle.py`:** 15 eski kart (900xxx) temizlendi → 29 yeni kart yüklendi → cozum.db: **144 kayıt** (taban KB + 29 kart), indeks tazelendi.

6. **Regresyon:** `benzer_anahtar_regresyon.py` → **BUL 16/16 · SUS 2/2 · TOPLAM 18/18** ✓

**Değişen dosyalar:** `.db` dosyaları gitignore'da, çalışma ağacında lokal olarak güncellendi — commit yapılmadı.

**Kalan iş:** Yok. Görev eksiksiz tamamlandı.
